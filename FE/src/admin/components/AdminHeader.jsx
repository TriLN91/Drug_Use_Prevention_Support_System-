export default function AdminHeader() {
  return (
    <header className="admin-header">
      <span>Drug Use Prevention - ADMIN PANEL</span>
      <span>Hello, Admin User <button>Log out</button></span>
    </header>
  );
}